import Layout from 'components/Layout';
import Head from 'next/head';

const Home: React.FC = () => {
    return (
        <>
            <Head>
                <title>Homepage</title>
            </Head>
            <Layout />
        </>
    );
};

export default Home;
